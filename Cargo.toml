[package]
name = "login-client"
version = "0.1.0"
authors = ["wagnrd"]
edition = "2021"
build = "build.rs"

[dependencies]
rand = "0.8.5"
reqwest = { version = "0.11.16", features = ["blocking", "json"] }
serde = { version = "1.0", features = ["derive"] }
slint = "1.0"

[build-dependencies]
slint-build = "1.0"
